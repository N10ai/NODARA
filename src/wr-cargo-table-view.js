import './handling-units-table-enhancer.js';
import './wr-guided-workspace.js';

const main=document.getElementById('main');
let activeRootId=null,installing=false,scheduled=false,lastSignature='',zeroInitialized=false;

const style=document.createElement('style');
style.textContent=`
.wr-cargo-list-shell{margin-top:14px}.wr-cargo-list-head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin:0 0 10px}.wr-cargo-list-head>div{min-width:0}.wr-cargo-list-head h4{margin:0;font-size:14px}.wr-cargo-list-head small{display:block;color:var(--muted);margin-top:2px}.wr-cargo-table-wrap{overflow-x:auto;border:1px solid var(--border);border-radius:14px}.wr-cargo-table{width:100%;border-collapse:collapse;min-width:880px}.wr-cargo-table th{font-size:9px;text-transform:uppercase;letter-spacing:.08em;color:var(--muted);text-align:left;padding:9px 11px;border-bottom:1px solid var(--border);background:color-mix(in srgb,var(--panel,#10141c) 92%,transparent)}.wr-cargo-table td{padding:10px 11px;border-bottom:1px solid color-mix(in srgb,var(--border) 75%,transparent);font-size:12px;vertical-align:middle}.wr-cargo-table tr:last-child td{border-bottom:0}.wr-cargo-row-main{display:flex;flex-direction:column;gap:2px;min-width:180px}.wr-cargo-row-main b{font-size:12px}.wr-cargo-row-main small{color:var(--muted)}.wr-cargo-row-actions{display:flex;gap:6px;justify-content:flex-end;flex-wrap:wrap}.wr-cargo-empty{padding:22px;text-align:center;color:var(--muted)}.wr-cargo-editor-head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin:10px 0 12px;padding:10px 0;border-bottom:1px solid var(--border)}.wr-cargo-editor-head>div{min-width:0}.wr-cargo-editor-head h4{margin:0;font-size:14px}.wr-cargo-editor-head small{display:block;color:var(--muted);margin-top:2px}.cargo-hierarchy-v3[data-table-editor-open="1"] .wr-cargo-list-shell{display:none!important}.cargo-hierarchy-v3[data-table-editor-open="1"] .cargo-root-list{display:block}.cargo-hierarchy-v3[data-table-editor-open="1"] .cargo-root-branch{display:none}.cargo-hierarchy-v3[data-table-editor-open="1"] .cargo-root-branch[data-table-active="1"]{display:block}.cargo-hierarchy-v3:not([data-table-editor-open="1"]) .cargo-root-list{display:none}.cargo-hierarchy-v3:not([data-table-editor-open="1"]) #cv3-add-root{display:none}@media(max-width:760px){.wr-cargo-list-head,.wr-cargo-editor-head{align-items:flex-start;flex-direction:column}.wr-cargo-list-head .primary,.wr-cargo-editor-head .primary{width:100%}}
`;
document.head.appendChild(style);
const text=v=>String(v??'').trim();const num=v=>{const n=Number(v);return Number.isFinite(n)?n:0};const fmt=v=>{const n=num(v);return n?Number(n.toFixed(3)).toLocaleString():'—'};const escapeHtml=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
function branches(h){return [...h.querySelectorAll(':scope > .cargo-root-list > .cargo-root-branch')]}function rootCard(b){return b?.querySelector(':scope > .cargo-tree-node')||b?.querySelector('.cargo-tree-node')||null}function field(c,s){return c?.querySelector(`[id$="-${s}"]`)||null}function rootId(b){const id=rootCard(b)?.querySelector('select[id$="-type"]')?.id||'';return id.startsWith('cv3-')?id.slice(4,-5):''}
function rootData(b,i){const c=rootCard(b),children=[...b.querySelectorAll('.cargo-child-node')],types=children.map(x=>field(x,'type')?.value).filter(Boolean),unique=[...new Set(types)],contents=children.length?`${children.length} nested ${children.length===1?'level/item':'levels/items'}${unique.length?` · ${unique.slice(0,3).join(', ')}`:''}`:'No nested contents',part=text(field(c,'part')?.value),sku=text(field(c,'sku')?.value),desc=text(field(c,'desc')?.value),barcode=text(field(c,'barcode')?.value),label=part||sku||desc||`${text(field(c,'type')?.value)||'Cargo'} ${i+1}`,secondary=[sku&&sku!==label?sku:null,desc&&desc!==label?desc:null,barcode].filter(Boolean).join(' · '),dims=[field(c,'l')?.value,field(c,'w')?.value,field(c,'h')?.value].map(text),du=document.getElementById('cv3-dim-unit')?.value||'IN',wu=document.getElementById('cv3-weight-unit')?.value||'KG';return{label,secondary,type:text(field(c,'type')?.value)||'—',qty:text(field(c,'qty')?.value)||'1',contents,weight:field(c,'weight')?.value?`${fmt(field(c,'weight')?.value)} ${wu}`:'—',dims:dims.every(Boolean)?`${dims.join(' × ')} ${du}`:'—',location:text(field(c,'location')?.value)||'—',condition:text(field(c,'condition')?.value)||'GOOD'}}
function blankRoot(b){const c=rootCard(b);return c&&['part','sku','desc','barcode','weight','l','w','h','location'].every(s=>!text(field(c,s)?.value))&&!b.querySelector('.cargo-child-node')}
function findActive(h){return activeRootId?branches(h).find(b=>rootId(b)===activeRootId):null}function signature(h){return branches(h).map((b,i)=>JSON.stringify(rootData(b,i))).join('|')+`|${activeRootId||''}`}
function closeEditor(h){activeRootId=null;h.dataset.tableEditorOpen='0';branches(h).forEach(b=>delete b.dataset.tableActive);h.querySelector('.wr-cargo-editor-head')?.remove();renderList(h)}function openEditor(h,b){activeRootId=rootId(b);h.dataset.tableEditorOpen='1';branches(h).forEach(x=>x.dataset.tableActive=x===b?'1':'0');renderEditorHeader(h,b)}
function renderEditorHeader(h,b){let head=h.querySelector('.wr-cargo-editor-head');const d=rootData(b,Math.max(0,branches(h).indexOf(b)));if(!head){head=document.createElement('div');head.className='wr-cargo-editor-head';h.querySelector('.cargo-root-list')?.insertAdjacentElement('beforebegin',head)}head.innerHTML=`<div><span class="cargo-node-eyebrow">CARGO EDITOR</span><h4>${escapeHtml(d.label)}</h4><small>Edit packaging, quantities, nested contents, identifiers, dimensions, location and condition.</small></div><button type="button" class="primary compact-btn" data-cargo-editor-done>Done</button>`;head.querySelector('[data-cargo-editor-done]').onclick=()=>closeEditor(h)}
function renderList(h){h.querySelector('.wr-cargo-editor-head')?.remove();let shell=h.querySelector('.wr-cargo-list-shell');if(!shell){shell=document.createElement('div');shell.className='wr-cargo-list-shell';h.querySelector('.cargo-root-list')?.insertAdjacentElement('beforebegin',shell)}const rows=branches(h);shell.innerHTML=`<div class="wr-cargo-list-head"><div><span class="cargo-node-eyebrow">CARGO / ITEMS</span><h4>${rows.length} line${rows.length===1?'':'s'}</h4><small>One cargo table. Click a line to open details.</small></div><button type="button" class="primary compact-btn" data-table-add-item>＋ Add item</button></div>${rows.length?`<div class="wr-cargo-table-wrap"><table class="wr-cargo-table"><thead><tr><th>Item / cargo</th><th>Package</th><th>Qty</th><th>Contents</th><th>Weight / pc</th><th>Dimensions</th><th>Location</th><th>Condition</th><th></th></tr></thead><tbody>${rows.map((b,i)=>{const d=rootData(b,i);return`<tr data-table-cargo-row="${i}"><td><div class="wr-cargo-row-main"><b>${escapeHtml(d.label)}</b><small>${escapeHtml(d.secondary||'Cargo line')}</small></div></td><td>${escapeHtml(d.type)}</td><td>${escapeHtml(d.qty)}</td><td>${escapeHtml(d.contents)}</td><td>${escapeHtml(d.weight)}</td><td>${escapeHtml(d.dims)}</td><td>${escapeHtml(d.location)}</td><td>${escapeHtml(d.condition)}</td><td><div class="wr-cargo-row-actions"><button type="button" class="subtle compact-btn" data-table-edit="${i}">Edit</button><button type="button" class="subtle compact-btn" data-table-copy="${i}">Duplicate</button><button type="button" class="subtle compact-btn" data-table-remove="${i}">Remove</button></div></td></tr>`}).join('')}</tbody></table></div>`:'<div class="wr-cargo-empty">No cargo lines yet. Add an item to begin receiving.</div>'}`;shell.querySelector('[data-table-add-item]').onclick=()=>{const before=branches(h).length;document.getElementById('cv3-add-root')?.click();setTimeout(()=>{const next=branches(h),b=next[Math.min(before,next.length-1)]||next.at(-1);if(b)openEditor(h,b)},40)};shell.querySelectorAll('[data-table-edit]').forEach(btn=>btn.onclick=e=>{e.stopPropagation();const b=branches(h)[Number(btn.dataset.tableEdit)];if(b)openEditor(h,b)});shell.querySelectorAll('[data-table-copy]').forEach(btn=>btn.onclick=e=>{e.stopPropagation();branches(h)[Number(btn.dataset.tableCopy)]?.querySelector('[data-duplicate-root]')?.click()});shell.querySelectorAll('[data-table-remove]').forEach(btn=>btn.onclick=e=>{e.stopPropagation();branches(h)[Number(btn.dataset.tableRemove)]?.querySelector('[data-remove-root]')?.click()});shell.querySelectorAll('[data-table-cargo-row]').forEach(row=>row.onclick=e=>{if(e.target.closest('button'))return;const b=branches(h)[Number(row.dataset.tableCargoRow)];if(b)openEditor(h,b)})}
function initializeZero(h){if(zeroInitialized)return;zeroInitialized=true;const rows=branches(h);if(rows.length===1&&blankRoot(rows[0]))rows[0].querySelector('[data-remove-root]')?.click()}
function install(){if(installing)return;const h=document.querySelector('.cargo-hierarchy-v3');if(!h)return;installing=true;try{if(h.dataset.tableViewInstalled!=='1'){h.dataset.tableViewInstalled='1';h.dataset.tableEditorOpen='0';initializeZero(h)}const current=branches(h),active=findActive(h);if(activeRootId&&!active)activeRootId=null;if(active){h.dataset.tableEditorOpen='1';current.forEach(b=>b.dataset.tableActive=b===active?'1':'0');renderEditorHeader(h,active)}else{h.dataset.tableEditorOpen='0';current.forEach(b=>delete b.dataset.tableActive);const sig=signature(h);if(sig!==lastSignature||!h.querySelector('.wr-cargo-list-shell')){lastSignature=sig;renderList(h)}}}finally{installing=false}}
function schedule(){if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;install()})}new MutationObserver(mutations=>{if(mutations.every(m=>m.target.closest?.('.wr-cargo-list-shell,.wr-cargo-editor-head')))return;schedule()}).observe(main,{childList:true,subtree:true});main.addEventListener('input',e=>{if(e.target.closest('.cargo-hierarchy-v3')&&!activeRootId)schedule()});main.addEventListener('change',e=>{if(e.target.closest('.cargo-hierarchy-v3')&&!activeRootId)schedule()});setTimeout(schedule,450);

const main=document.getElementById('main');
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
function activeCargoId(){const title=document.querySelector('#wrc-drawer .wrc-drawer-head h2')?.textContent?.trim();if(!title)return null;const row=[...main.querySelectorAll('.wrc-table tbody tr')].find(r=>r.querySelector('.wrc-tree b')?.textContent?.trim()===title);return row?.querySelector('[data-edit]')?.dataset.edit||null}
async function ensureInput(){let input=document.getElementById('wrc-file-input');if(input)return input;const docs=main.querySelector('[data-step="documents"]');if(!docs)return null;docs.click();for(let i=0;i<20;i++){await sleep(50);input=document.getElementById('wrc-file-input');if(input)return input}return null}
async function capture(photo){if(main.dataset.wrDraft!=='1')return;const cargoId=activeCargoId(),input=await ensureInput();if(!input){alert('File capture is not available on this draft.');return}input.accept=photo?'image/*':'image/*,application/pdf';input.setAttribute('capture','environment');input.value='';const back=()=>{const cargo=main.querySelector('[data-step="cargo"]');cargo?.click();if(cargoId)setTimeout(()=>main.querySelector(`[data-edit="${CSS.escape(cargoId)}"]`)?.click(),120)};input.addEventListener('change',()=>setTimeout(back,100),{once:true});input.click()}
document.addEventListener('click',e=>{const photo=e.target.closest?.('[data-wrc-photo]'),scan=e.target.closest?.('[data-wrc-scan],[data-wrc-scan-doc]');if(!photo&&!scan)return;if(main.dataset.wrDraft!=='1')return;e.preventDefault();e.stopImmediatePropagation();capture(!!photo)},{capture:true});

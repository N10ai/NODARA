const main=document.getElementById('main');
const tabState={entity:'identity',wr:'overview'};
function applyGroup(kind,tabs,sections){const active=tabState[kind]||sections[0]?.id;if(!active)return;sections.forEach(s=>s.hidden=s.id!==active);tabs.forEach(b=>b.classList.toggle('active',b.dataset.jump===active))}
function initEntity(){const bar=main.querySelector('.entity-tabs');if(!bar||bar.dataset.tabController)return;bar.dataset.tabController='1';const tabs=[...bar.querySelectorAll('[data-jump]')],sections=['identity','contacts','addresses','sop','rates'].map(id=>document.getElementById(id)).filter(Boolean);applyGroup('entity',tabs,sections);tabs.forEach(b=>b.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();tabState.entity=b.dataset.jump;applyGroup('entity',tabs,sections)},true))}
function initWR(){const head=main.querySelector('.wr-object-head');if(!head)return;const bar=head.nextElementSibling;if(!bar?.classList.contains('record-tabs')||bar.dataset.tabController)return;bar.dataset.tabController='1';const tabs=[...bar.querySelectorAll('[data-jump]')];if(!tabs.length)return;const sections=['overview','references','cargo','documents','charges'].map(id=>document.getElementById(id)).filter(Boolean);applyGroup('wr',tabs,sections);tabs.forEach(b=>b.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();tabState.wr=b.dataset.jump;applyGroup('wr',tabs,sections)},true))}
function install(){initEntity();initWR()}
let queued=false;new MutationObserver(()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;install()})}).observe(main,{childList:true,subtree:true});setTimeout(install,250);

// Shipment cohesion guard: one canonical record, no legacy edit layer.
const main=document.getElementById('main');
let cargoMounting=false;
function canonical(){return main?.querySelector('.shipment-native-shell')}
function removeLegacyEdit(){const shell=canonical();if(!shell)return;main.querySelector('#sh-edit')?.remove();main.querySelectorAll(':scope > .record-header,:scope > .record-section,#shipment-mode-record').forEach(x=>x.remove())}
function tightenPartyMenus(){const shell=canonical();if(!shell)return;shell.querySelectorAll('.shipment-parties .nr-resolver').forEach(root=>{if(root.dataset.shipmentTight==='1')return;root.dataset.shipmentTight='1';const input=root.querySelector('.nr-input'),menu=root.querySelector('.nr-menu');if(!input||!menu)return;const sync=()=>{if(input.value.trim().length<1)menu.hidden=true};input.addEventListener('focus',sync,true);input.addEventListener('input',sync,true)})}
async function cargoGuard(){const shell=canonical();if(!shell||main.dataset.txwTab!=='cargo'||cargoMounting)return;const host=shell.querySelector('[data-shipment-cargo-host]');if(!host)return;host.querySelectorAll('.cargo-ws').forEach((x,i)=>{if(i>0)x.remove()});if(host.querySelector('.cargo-ws'))return;const id=(shell.dataset.txwShell||'').split(':')[1];if(!id||!window.nodaraCargoWorkspace?.mount)return;cargoMounting=true;try{await window.nodaraCargoWorkspace.mount(host,{transactionType:'SHIPMENT',transactionId:id,title:'Shipment Cargo',allowAdd:true})}finally{cargoMounting=false}}
function reconcile(){removeLegacyEdit();tightenPartyMenus();cargoGuard()}
document.addEventListener('click',e=>{const edit=e.target.closest?.('#sh-edit');if(edit&&canonical()){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();canonical().querySelector('[data-txw-tab="overview"]')?.click();return}},true);
if(main)new MutationObserver(()=>queueMicrotask(reconcile)).observe(main,{childList:true,subtree:true});queueMicrotask(reconcile);window.nodaraShipmentCohesionV48={reconcile};

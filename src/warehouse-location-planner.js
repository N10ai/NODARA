import { supabase } from './supabase-client.js';
const main=document.getElementById('main');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const pad=(n,w)=>String(n).padStart(Math.max(1,Number(w)||1),'0');
async function org(){const{data,error}=await supabase.rpc('bootstrap_workspace',{p_name:'NODARA Workspace'});if(error)throw error;return data}
function values(){return{facility:val('lp-facility')||'Main Warehouse',zone:val('lp-zone').toUpperCase()||'A',includeZone:chk('lp-include-zone'),rackPrefix:val('lp-rack-prefix').toUpperCase(),bayPrefix:val('lp-bay-prefix').toUpperCase(),levelPrefix:val('lp-level-prefix').toUpperCase(),positionPrefix:val('lp-position-prefix').toUpperCase(),separator:document.getElementById('lp-separator')?.value??'-',padding:Math.max(1,Number(val('lp-padding')||2)),rackStart:Math.max(1,Number(val('lp-rack-start')||1)),racks:Math.max(1,Number(val('lp-racks')||1)),bays:Math.max(1,Number(val('lp-bays')||1)),levels:Math.max(1,Number(val('lp-levels')||1)),positions:Math.max(1,Number(val('lp-positions')||1)),capacity:Math.max(1,Number(val('lp-capacity')||1)),length:numOrNull('lp-length'),width:numOrNull('lp-width'),height:numOrNull('lp-height'),ftz:chk('lp-ftz'),bonded:chk('lp-bonded'),restricted:chk('lp-restricted'),labelType:document.getElementById('lp-label-type')?.value||'CODE128'}}
const val=id=>document.getElementById(id)?.value?.trim()||'';const chk=id=>!!document.getElementById(id)?.checked;const numOrNull=id=>val(id)===''?null:Number(val(id));
function component(prefix,n,v){return `${prefix}${pad(n,v.padding)}`}
function positionRows(v){const out=[];for(let r=0;r<v.racks;r++){const rack=component(v.rackPrefix,v.rackStart+r,v);for(let b=1;b<=v.bays;b++){const bay=component(v.bayPrefix,b,v);for(let l=1;l<=v.levels;l++){const level=component(v.levelPrefix,l,v);for(let p=1;p<=v.positions;p++){const position=component(v.positionPrefix,p,v),parts=[v.includeZone?v.zone:null,rack,bay,level,position].filter(x=>x!==null&&x!=='');out.push({code:parts.join(v.separator),rack,bay,level,position})}}}}return out}
function preview(){const v=values(),rows=positionRows(v),el=document.getElementById('lp-preview');if(!el)return;el.innerHTML=`<div class="location-kpis"><div class="location-kpi"><span>Racks</span><strong>${v.racks}</strong></div><div class="location-kpi"><span>Bays</span><strong>${v.racks*v.bays}</strong></div><div class="location-kpi"><span>Levels</span><strong>${v.racks*v.bays*v.levels}</strong></div><div class="location-kpi location-kpi-primary"><span>Assignable positions</span><strong>${rows.length}</strong></div></div><div class="notice"><b>Only ${rows.length} assignable position records will be created.</b><small>Rack, bay and level are stored as structure attributes, not extra locations. Example: ${esc(rows.slice(0,5).map(x=>x.code).join(' · '))}${rows.length>5?' …':''}</small></div>`}
export function openLocationPlanner(){window.nodaraSetActive?.('warehouse_locations');main.innerHTML=`<div class="record-commandbar"><button class="secondary compact-btn" id="lp-back">‹ Locations</button></div><div class="eyebrow">Warehouse · Location Planner</div><div class="location-titlebar"><div><h1 class="title">Build rack positions</h1><p class="muted">Generate only real assignable locations. Rack/bay/level remain structural attributes for maps, filtering and optimization.</p></div></div><section class="record-section"><div class="section-heading"><div><h3>Structure</h3><span class="muted">Configure the physical pattern and your preferred location-code format.</span></div></div><div class="detail-grid"><div class="field"><label>Facility</label><input id="lp-facility" value="Main Warehouse"></div><div class="field"><label>Zone / Area</label><input id="lp-zone" value="A"></div><div class="field"><label>Starting rack #</label><input id="lp-rack-start" type="number" min="1" value="1"></div><div class="field"><label>Rack quantity</label><input id="lp-racks" type="number" min="1" value="4"></div><div class="field"><label>Bays per rack</label><input id="lp-bays" type="number" min="1" value="10"></div><div class="field"><label>Levels per bay</label><input id="lp-levels" type="number" min="1" value="3"></div><div class="field"><label>Positions per level</label><input id="lp-positions" type="number" min="1" value="2"></div><div class="field"><label>Capacity per position</label><input id="lp-capacity" type="number" min="1" value="1"></div></div></section><section class="record-section"><div class="section-heading"><div><h3>Location code format</h3><span class="muted">Prefixes can be blank. Example with no prefixes: 01-03-02-01.</span></div></div><div class="detail-grid"><div class="field"><label>Rack prefix</label><input id="lp-rack-prefix" value="R"></div><div class="field"><label>Bay prefix</label><input id="lp-bay-prefix" value="B"></div><div class="field"><label>Level prefix</label><input id="lp-level-prefix" value="L"></div><div class="field"><label>Position prefix</label><input id="lp-position-prefix" value="P"></div><div class="field"><label>Separator</label><input id="lp-separator" value="-"></div><div class="field"><label>Number padding</label><input id="lp-padding" type="number" min="1" max="5" value="2"></div><div class="field"><label>Label barcode</label><select id="lp-label-type"><option value="CODE128">Code 128</option><option value="QR">QR code</option></select></div></div><div class="location-control-grid"><label><input id="lp-include-zone" type="checkbox" checked> Include zone in code</label><label><input id="lp-ftz" type="checkbox"> FTZ controlled</label><label><input id="lp-bonded" type="checkbox"> Bonded</label><label><input id="lp-restricted" type="checkbox"> Restricted</label></div></section><section class="record-section"><div class="section-heading"><div><h3>Position dimensions</h3></div></div><div class="detail-grid"><div class="field"><label>Usable L (in)</label><input id="lp-length" type="number" min="0"></div><div class="field"><label>Usable W (in)</label><input id="lp-width" type="number" min="0"></div><div class="field"><label>Usable H (in)</label><input id="lp-height" type="number" min="0"></div></div></section><div id="lp-preview"></div><div class="record-footer-actions"><button class="secondary" id="lp-print-preview">Preview labels</button><button class="primary" id="lp-generate">Generate positions</button></div>`;document.getElementById('lp-back').onclick=()=>window.nodaraLocations?.();main.querySelectorAll('input,select').forEach(x=>x.addEventListener('input',preview));main.querySelectorAll('select').forEach(x=>x.addEventListener('change',preview));document.getElementById('lp-print-preview').onclick=()=>printLocationLabels(positionRows(values()).map(x=>x.code),values().labelType);document.getElementById('lp-generate').onclick=generate;preview()}
async function generate(){const v=values(),rows=positionRows(v);if(rows.length>3000&&!confirm(`This will create ${rows.length} assignable positions. Continue?`))return;const btn=document.getElementById('lp-generate');btn.disabled=true;btn.textContent='Checking…';try{const organization_id=await org(),codes=rows.map(x=>x.code),{data:existing,error}=await supabase.from('warehouse_locations').select('code').eq('organization_id',organization_id).in('code',codes.slice(0,1000));if(error)throw error;const dupes=(existing||[]).map(x=>x.code);if(dupes.length)throw new Error(`Location codes already exist (${dupes.slice(0,5).join(', ')}${dupes.length>5?'…':''}). Change the format/range or remove old test locations first.`);btn.textContent='Generating…';const common={organization_id,facility:v.facility,zone:v.zone,active:true,ftz:v.ftz,bonded:v.bonded,restricted:v.restricted,type:'POSITION',space_class:'STORAGE',capacity:v.capacity,capacity_uom:'POS',length_in:v.length,width_in:v.width,height_in:v.height,putaway_enabled:true,picking_enabled:true};for(let i=0;i<rows.length;i+=500){const batch=rows.slice(i,i+500).map(x=>({...common,code:x.code,name:x.code,rack_code:x.rack,bay_code:x.bay,level_code:x.level,position_code:x.position}));const{error:e}=await supabase.from('warehouse_locations').insert(batch);if(e)throw e}btn.textContent='Created';if(confirm(`${rows.length} assignable positions created. Print location labels now?`))printLocationLabels(codes,v.labelType);await window.nodaraLocations?.()}catch(e){alert(e.message);btn.disabled=false;btn.textContent='Generate positions'}}
export function printLocationLabels(codes=[],type='CODE128'){if(!codes.length)return alert('No locations to print.');const w=window.open('','_blank','width=900,height=700');if(!w)return alert('Allow pop-ups to print labels.');const qr=type==='QR';w.document.write(`<!doctype html><html><head><title>Location Labels</title><style>@page{size:4in 2in;margin:.08in}body{margin:0;font-family:Arial,sans-serif}.label{width:3.84in;height:1.84in;box-sizing:border-box;display:flex;align-items:center;justify-content:center;gap:16px;page-break-after:always}.human{font-size:25pt;font-weight:800;line-height:1.05;word-break:break-all}.meta{font-size:8pt;margin-top:5px}.barcode{width:2.1in;height:.75in}.qr{width:1.15in;height:1.15in}</style>${qr?'':'<script src="https://cdn.jsdelivr.net/npm/jsbarcode@3.11.6/dist/JsBarcode.all.min.js"><\/script>'}${qr?'<script src="https://cdn.jsdelivr.net/npm/qrcodejs@1.0.0/qrcode.min.js"><\/script>':''}</head><body>${codes.map((c,i)=>`<div class="label"><div><div class="human">${esc(c)}</div><div class="meta">NODARA · WAREHOUSE LOCATION</div></div>${qr?`<div class="qr" id="q${i}"></div>`:`<svg class="barcode" id="b${i}"></svg>`}</div>`).join('')}<script>onload=()=>{${qr?codes.map((c,i)=>`new QRCode(document.getElementById('q${i}'),{text:${JSON.stringify(c)},width:110,height:110,correctLevel:QRCode.CorrectLevel.M});`).join(''):codes.map((c,i)=>`JsBarcode('#b${i}',${JSON.stringify(c)},{format:'CODE128',displayValue:false,margin:0,height:55,width:2});`).join('')}setTimeout(()=>print(),400)}<\/script></body></html>`);w.document.close()}
function inject(){const add=document.getElementById('loc-new');if(!add||document.getElementById('loc-plan'))return;const b=document.createElement('button');b.id='loc-plan';b.className='secondary';b.textContent='▦ Plan / Generate';b.onclick=openLocationPlanner;add.before(b)}let q=false;new MutationObserver(()=>{if(q)return;q=true;requestAnimationFrame(()=>{q=false;inject()})}).observe(main,{childList:true,subtree:true});setTimeout(inject,450);window.nodaraLocationPlanner=openLocationPlanner;

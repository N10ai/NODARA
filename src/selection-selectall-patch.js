/* Deprecated compatibility shim.
   Selection is now rendered by each table itself (WR/data-view pattern).
   Keeping this module intentionally inert prevents MutationObserver feedback loops
   and duplicate selector bars on CR, Rates, Locations and Entities. */

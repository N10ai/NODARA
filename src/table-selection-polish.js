import { supabase } from './supabase-client.js';
const selected={rates:new Set(),locations:new Set()};

function headWrap(kind,host,head){let wrap=host.querySelector(`[data-inline-select-head="${kind}"]`);if(wrap)return wrap;wrap=document.createElement('div');wrap.className='record-select-row record-select-head-row';wrap.dataset.inlineSelectHead=kind;const label=document.createElement('label');label.className='record-select-check';label.innerHTML='<input type="checkbox" aria-label="Select visible records">';head.parentNode.insertBefore(wrap,head);wrap.append(label,head);label.querySelector('input').onchange=e=>{const on=e.target.checked;for(const w of host.querySelectorAll(`.record-select-row[data-select-kind="${kind}"]`)){const id=w.dataset.selectId;on?selected[kind].add(id):selected[kind].delete(id)}sync(kind,host)};return wrap}
function decorate(kind,host,rows,idOf,head){for(const row of rows){if(row.closest('.record-select-row'))continue;const id=idOf(row);if(!id)continue;const wrap=document.createElement('div');wrap.className='record-select-row';wrap.dataset.selectKind=kind;wrap.dataset.selectId=id;const box=document.createElement('label');box.className='record-select-check';box.innerHTML=`<input type="checkbox" ${selected[kind].has(id)?'checked':''} aria-label="Select record">`;row.parentNode.insertBefore(wrap,row);wrap.append(box,row)}if(head&&!head.closest('.record-select-head-row'))headWrap(kind,host,head);if(!host.dataset[`inlineBound${kind}`]){host.dataset[`inlineBound${kind}`]='1';host.addEventListener('click',e=>{if(e.target.closest('.record-select-check'))e.stopPropagation()});host.addEventListener('change',e=>{const x=e.target.closest(`.record-select-row[data-select-kind="${kind}"] .record-select-check input`);if(!x)return;const w=x.closest('.record-select-row'),id=w.dataset.selectId;x.checked?selected[kind].add(id):selected[kind].delete(id);sync(kind,host);paintLocationActions()})}sync(kind,host)}
function sync(kind,host){const wraps=[...host.querySelectorAll(`.record-select-row[data-select-kind="${kind}"]`)];for(const w of wraps){const on=selected[kind].has(w.dataset.selectId),x=w.querySelector('.record-select-check input');if(x)x.checked=on;w.classList.toggle('selected',on)}const h=host.querySelector(`[data-inline-select-head="${kind}"] input`);if(h){const n=wraps.filter(w=>selected[kind].has(w.dataset.selectId)).length;h.checked=wraps.length>0&&n===wraps.length;h.indeterminate=n>0&&n<wraps.length}}
function enhanceRates(){if(window.__nodaraRoute!=='pricing_rates')return;const list=document.querySelector('.rate-book-section .rate-list');if(!list)return;let head=list.querySelector(':scope > .rate-select-head');if(!head){head=document.createElement('div');head.className='rate-select-head';head.innerHTML='<span>Rate</span>';list.prepend(head)}decorate('rates',list,[...list.querySelectorAll(':scope > .rate-row')],r=>r.dataset.rateService,head)}
function enhanceLocations(){if(!['warehouse_locations','settings_locations'].includes(window.__nodaraRoute))return;const table=document.querySelector('#loc-list .location-table');if(!table)return;const head=table.querySelector(':scope > .location-head');decorate('locations',table,[...table.querySelectorAll(':scope > [data-loc-open]')],r=>r.dataset.locOpen,head);paintLocationActions()}
function paintLocationActions(){if(!['warehouse_locations','settings_locations'].includes(window.__nodaraRoute))return;let bar=document.querySelector('[data-location-selection-actions]');const title=document.querySelector('.location-titlebar');if(!title)return;if(!bar){bar=document.createElement('div');bar.dataset.locationSelectionActions='1';bar.className='view-selection-bar';title.insertAdjacentElement('afterend',bar)}const n=selected.locations.size;bar.innerHTML=`<div><b>${n} selected</b><small>Locations</small></div><div class="view-selection-actions"><button class="subtle compact-btn" data-loc-select-all>Select all</button><button class="danger-btn compact-btn" data-loc-delete-selected ${n?'':'disabled'}>Delete selected</button><button class="subtle compact-btn" data-loc-clear>Clear</button></div>`;bar.querySelector('[data-loc-select-all]').onclick=()=>{document.querySelectorAll('.record-select-row[data-select-kind="locations"]').forEach(w=>selected.locations.add(w.dataset.selectId));sync('locations',document.querySelector('#loc-list .location-table'));paintLocationActions()};bar.querySelector('[data-loc-clear]').onclick=()=>{selected.locations.clear();sync('locations',document.querySelector('#loc-list .location-table'));paintLocationActions()};bar.querySelector('[data-loc-delete-selected]').onclick=deleteLocations}
async function deleteLocations(){const ids=[...selected.locations];if(!ids.length||!confirm(`Delete ${ids.length} selected location${ids.length===1?'':'s'}? Locations currently assigned to cargo will be protected.`))return;const{data:used,error:ue}=await supabase.from('cargo_units').select('warehouse_location_id').in('warehouse_location_id',ids);if(ue)return alert(ue.message);const blocked=new Set((used||[]).map(x=>x.warehouse_location_id).filter(Boolean)),deletable=ids.filter(id=>!blocked.has(id));if(deletable.length){await supabase.from('warehouse_space_allocations').delete().in('location_id',deletable);const{error}=await supabase.from('warehouse_locations').delete().in('id',deletable);if(error)return alert(error.message)}selected.locations.clear();if(blocked.size)alert(`${deletable.length} deleted. ${blocked.size} location${blocked.size===1?' is':'s are'} still assigned to cargo and were kept.`);await window.nodaraLocations?.()}
function enhance(){enhanceRates();enhanceLocations()}
let lastRoute='',queued=false;const schedule=()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;const route=window.__nodaraRoute||'';if(route!==lastRoute){selected.rates.clear();selected.locations.clear();lastRoute=route}enhance()})};new MutationObserver(schedule).observe(document.getElementById('main'),{childList:true,subtree:true});window.addEventListener('nodara:rate-view-changed',schedule);setTimeout(schedule,250);

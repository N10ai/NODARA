import { supabase } from './supabase-client.js';
import { getCurrentOrganizationId } from './live-data.js';

const main=document.getElementById('main');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const css=document.createElement('style');css.textContent=`
.settings-system{max-width:1120px;margin:0 auto}.settings-head{display:flex;justify-content:space-between;gap:16px;align-items:flex-start;margin-bottom:18px}.settings-head h1{margin:4px 0 5px}.settings-head p{margin:0;color:var(--muted)}.settings-tabs{display:flex;gap:8px;overflow:auto;margin:16px 0 20px}.settings-tab{border:1px solid var(--border);background:transparent;color:var(--muted);padding:9px 13px;border-radius:999px;white-space:nowrap}.settings-tab.active{background:#182235;color:#fff;border-color:#3e5d86}.settings-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.settings-card{border:1px solid var(--border);background:var(--panel,#0e131a);border-radius:20px;padding:17px}.settings-card h3{margin:0 0 3px}.settings-card>p{margin:0 0 16px;color:var(--muted);font-size:12px}.settings-fields{display:grid;grid-template-columns:1fr 1fr;gap:12px}.settings-field label{display:block;font-size:11px;color:var(--muted);margin-bottom:5px}.settings-field input,.settings-field select{width:100%;box-sizing:border-box}.settings-span2{grid-column:1/-1}.number-row{border:1px solid var(--border);border-radius:16px;padding:14px;margin:10px 0}.number-row-head{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-bottom:11px}.number-row-head b{font-size:15px}.number-preview{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;color:#8cc0ff;font-size:12px;margin-top:8px}.settings-savebar{position:sticky;bottom:calc(76px + env(safe-area-inset-bottom));margin-top:16px;padding:10px;border:1px solid var(--border);border-radius:16px;background:color-mix(in srgb,var(--panel,#0e131a) 92%,transparent);backdrop-filter:blur(16px);display:flex;justify-content:flex-end;gap:8px;z-index:20}.settings-savebar button{min-width:150px}.settings-note{font-size:11px;color:var(--muted);margin-top:8px}
.workflow-toolbar{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-bottom:14px}.workflow-toolbar select{min-width:240px}.workflow-canvas{border:1px solid var(--border);background:radial-gradient(circle at 15% 10%,#17345833,transparent 30%),#071018;border-radius:24px;padding:22px;overflow:auto;min-height:420px}.workflow-canvas-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start;margin-bottom:18px}.workflow-canvas-head h2{margin:3px 0 4px}.workflow-canvas-head p{margin:0;color:var(--muted);font-size:12px}.workflow-chain{display:flex;align-items:center;gap:10px;min-width:max-content;padding:20px 4px 26px}.workflow-node{width:210px;border:1px solid #29405c;background:linear-gradient(180deg,#101b29,#0a121c);border-radius:20px;padding:14px;box-shadow:0 14px 38px #0005;position:relative}.workflow-node.disabled{opacity:.45}.workflow-node.required{border-color:#3b7dcc}.workflow-node-top{display:flex;justify-content:space-between;gap:8px;align-items:flex-start}.workflow-node-type{font-size:9px;letter-spacing:.1em;color:#77a9da;text-transform:uppercase}.workflow-node h4{font-size:16px;margin:5px 0 4px}.workflow-node p{font-size:11px;line-height:1.35;color:var(--muted);margin:0;min-height:30px}.workflow-node-meta{display:flex;gap:5px;flex-wrap:wrap;margin-top:10px}.workflow-pill{font-size:9px;padding:4px 7px;border:1px solid #2c425d;border-radius:999px;color:#a9bfd6;background:#0a1521}.workflow-pill.req{color:#8bc2ff;border-color:#315f91}.workflow-node-actions{display:flex;gap:5px;margin-top:12px}.workflow-node-actions button{min-width:0;padding:6px 8px;border-radius:9px;background:#0d1926;border:1px solid #2b4059;color:#c6d5e5}.workflow-edge{width:34px;height:2px;background:#30475f;position:relative;flex:none}.workflow-edge:after{content:'›';position:absolute;right:-2px;top:-11px;color:#5f87b0;font-size:18px}.workflow-empty{padding:34px;text-align:center;color:var(--muted)}.workflow-inspector{margin-top:14px;border:1px solid var(--border);border-radius:18px;padding:15px;background:#0b121b}.workflow-inspector h3{margin:0 0 12px}.workflow-inspector-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.workflow-inspector-grid label{font-size:10px;color:var(--muted)}.workflow-inspector-grid input,.workflow-inspector-grid select,.workflow-inspector-grid textarea{width:100%;box-sizing:border-box;margin-top:5px}.workflow-inspector-grid textarea{min-height:76px}.workflow-add{border:1px dashed #3b5c80;border-radius:16px;background:#0b1622;color:#8fc0f2;min-width:120px;padding:14px}.workflow-legend{display:flex;gap:10px;flex-wrap:wrap;font-size:10px;color:var(--muted);margin-top:10px}
@media(max-width:760px){.settings-head{display:block}.settings-grid,.settings-fields,.workflow-inspector-grid{grid-template-columns:1fr}.settings-span2{grid-column:auto}.settings-card{padding:14px}.number-row-head{align-items:flex-start}.settings-savebar{bottom:calc(88px + env(safe-area-inset-bottom))}.workflow-toolbar{align-items:stretch;flex-direction:column}.workflow-toolbar select{min-width:0;width:100%}.workflow-canvas{padding:15px}.workflow-chain{align-items:stretch;flex-direction:column;min-width:0}.workflow-node{width:auto}.workflow-edge{width:2px;height:24px;margin-left:24px}.workflow-edge:after{content:'⌄';right:auto;left:-7px;top:8px}.workflow-add{width:100%}}
`;document.head.appendChild(css);

const UNIT_CATS=['WEIGHT','DIMENSION','VOLUME','TEMPERATURE','DISTANCE','COUNT','PACKAGE'];
let state={tab:'units',org:null,catalog:[],profile:null,schemes:[],workflows:[],workflow:null,selectedStep:null};
async function load(){
 state.org=await getCurrentOrganizationId();
 const [u,p,s,w]=await Promise.all([
  supabase.from('unit_catalog').select('code,name,symbol,category,active').eq('active',true).order('category').order('name'),
  supabase.from('organization_unit_profiles').select('*').eq('organization_id',state.org).eq('is_default',true).maybeSingle(),
  supabase.from('document_number_schemes').select('*').eq('organization_id',state.org).order('document_type'),
  supabase.from('workflow_definitions').select('*').eq('organization_id',state.org).order('transaction_type')
 ]);
 if(u.error)throw u.error;if(p.error)throw p.error;if(s.error)throw s.error;if(w.error)throw w.error;
 state.catalog=u.data||[];state.profile=p.data||{organization_id:state.org,profile_name:'Default',system_code:'IMPERIAL',is_default:true,weight_unit:'LB',dimension_unit:'IN',volume_unit:'CBM',temperature_unit:'F',distance_unit:'MI',default_quantity_uom:'PCS',currency_code:'USD',rounding:{weight:2,dimension:2,volume:3,currency:2}};state.schemes=s.data||[];state.workflows=w.data||[];state.workflow=state.workflow&&state.workflows.find(x=>x.id===state.workflow.id)||state.workflows.find(x=>x.transaction_type==='WAREHOUSE_RECEIPT')||state.workflows[0]||null;
}
const unitOpts=(cat,val)=>state.catalog.filter(x=>x.category===cat).map(x=>`<option value="${esc(x.code)}" ${x.code===val?'selected':''}>${esc(x.name)}${x.symbol?` · ${esc(x.symbol)}`:''}</option>`).join('');
function shell(){window.nodaraSetActive?.('settings_system');main.innerHTML=`<section class="settings-system"><div class="settings-head"><div><div class="eyebrow">SETTINGS</div><h1>System setup</h1><p>Organization-level operating rules. Each company keeps its own units, numbering and workflows.</p></div></div><div class="settings-tabs"><button class="settings-tab ${state.tab==='units'?'active':''}" data-stab="units">Units & formats</button><button class="settings-tab ${state.tab==='numbers'?'active':''}" data-stab="numbers">Document numbering</button><button class="settings-tab ${state.tab==='workflows'?'active':''}" data-stab="workflows">Workflows</button></div><div id="settings-body"></div></section>`;main.querySelectorAll('[data-stab]').forEach(b=>b.onclick=()=>{state.tab=b.dataset.stab;shell();renderBody()});renderBody()}
function units(){const p=state.profile,r=p.rounding||{};return `<div class="settings-grid"><section class="settings-card"><h3>Measurement system</h3><p>These are the default units shown when people create and edit cargo.</p><div class="settings-fields"><div class="settings-field settings-span2"><label>System preset</label><select id="set-system"><option ${p.system_code==='IMPERIAL'?'selected':''}>IMPERIAL</option><option ${p.system_code==='METRIC'?'selected':''}>METRIC</option><option ${p.system_code==='CUSTOM'?'selected':''}>CUSTOM</option></select></div><div class="settings-field"><label>Weight</label><select id="set-weight">${unitOpts('WEIGHT',p.weight_unit)}</select></div><div class="settings-field"><label>Dimensions</label><select id="set-dimension">${unitOpts('DIMENSION',p.dimension_unit)}</select></div><div class="settings-field"><label>Volume</label><select id="set-volume">${unitOpts('VOLUME',p.volume_unit)}</select></div><div class="settings-field"><label>Temperature</label><select id="set-temperature">${unitOpts('TEMPERATURE',p.temperature_unit)}</select></div><div class="settings-field"><label>Distance</label><select id="set-distance">${unitOpts('DISTANCE',p.distance_unit)}</select></div><div class="settings-field"><label>Default quantity UOM</label><select id="set-qty">${unitOpts('COUNT',p.default_quantity_uom)}${unitOpts('PACKAGE',p.default_quantity_uom)}</select></div><div class="settings-field settings-span2"><label>Currency</label><input id="set-currency" value="${esc(p.currency_code||'USD')}" maxlength="3"></div></div></section><section class="settings-card"><h3>Rounding & display</h3><p>Keep calculations precise internally while controlling what users see.</p><div class="settings-fields"><div class="settings-field"><label>Weight decimals</label><input id="set-rw" type="number" min="0" max="6" value="${Number(r.weight??2)}"></div><div class="settings-field"><label>Dimension decimals</label><input id="set-rd" type="number" min="0" max="6" value="${Number(r.dimension??2)}"></div><div class="settings-field"><label>Volume decimals</label><input id="set-rv" type="number" min="0" max="6" value="${Number(r.volume??3)}"></div><div class="settings-field"><label>Currency decimals</label><input id="set-rc" type="number" min="0" max="4" value="${Number(r.currency??2)}"></div></div><div class="settings-note">NODARA can still convert between units; this profile controls the default operating/display units.</div></section></div><div class="settings-savebar"><button class="primary" id="save-units">Save system units</button></div>`}
const sample=s=>{const d=new Date(),yy=String(d.getFullYear()).slice(-2),yyyy=d.getFullYear(),mm=String(d.getMonth()+1).padStart(2,'0'),dd=String(d.getDate()).padStart(2,'0');let date=(s.date_format||'YYMMDD').replace('YYYY',yyyy).replace('YY',yy).replace('MM',mm).replace('DD',dd);const seq=String(s.starting_number||1).padStart(Number(s.sequence_padding||4),'0');return (s.format_template||'{PREFIX}-{DATE}-{SEQ}{SUFFIX}').replace('{PREFIX}',s.prefix||'').replace('{DATE}',date).replace('{SEQ}',seq).replace('{SUFFIX}',s.suffix||'').replace(/--+/g,'-').replace(/^-|-$/g,'')};
function numbers(){return `<section class="settings-card"><h3>Operational document numbers</h3><p>Edit the numbering scheme for WRs, CRs, shipments, transport orders, quotes and other NODARA records.</p>${state.schemes.map((s,i)=>`<div class="number-row" data-scheme="${esc(s.id)}"><div class="number-row-head"><div><b>${esc(s.name||s.document_type)}</b><div class="settings-note">${esc(s.document_type)}</div></div><label><input type="checkbox" data-k="is_active" ${s.is_active?'checked':''}> Active</label></div><div class="settings-fields"><div class="settings-field"><label>Prefix</label><input data-k="prefix" value="${esc(s.prefix||'')}"></div><div class="settings-field"><label>Suffix</label><input data-k="suffix" value="${esc(s.suffix||'')}"></div><div class="settings-field settings-span2"><label>Format</label><input data-k="format_template" value="${esc(s.format_template||'{PREFIX}-{DATE}-{SEQ}{SUFFIX}')}"></div><div class="settings-field"><label>Date format</label><select data-k="date_format"><option ${s.date_format==='YYMMDD'?'selected':''}>YYMMDD</option><option ${s.date_format==='YYYYMMDD'?'selected':''}>YYYYMMDD</option><option ${s.date_format==='YYMM'?'selected':''}>YYMM</option><option ${s.date_format==='YYYY'?'selected':''}>YYYY</option></select></div><div class="settings-field"><label>Sequence digits</label><input data-k="sequence_padding" type="number" min="1" max="12" value="${s.sequence_padding||4}"></div><div class="settings-field"><label>Reset</label><select data-k="reset_period">${['NONE','YEAR','MONTH','DAY'].map(x=>`<option ${s.reset_period===x?'selected':''}>${x}</option>`).join('')}</select></div><div class="settings-field"><label>Start at</label><input data-k="starting_number" type="number" min="1" value="${s.starting_number||1}"></div><div class="settings-field settings-span2"><label><input type="checkbox" data-k="allow_manual_override" ${s.allow_manual_override?'checked':''}> Allow manual number override</label></div></div><div class="number-preview" data-preview>Preview · ${esc(sample(s))}</div></div>`).join('')||'<div class="notice">No numbering schemes are configured yet.</div>'}</section><div class="settings-savebar"><button class="primary" id="save-numbers">Save numbering</button></div>`}
function normalizedSteps(){return [...(state.workflow?.steps||[])].sort((a,b)=>Number(a.order||0)-Number(b.order||0))}
function workflows(){const w=state.workflow,steps=normalizedSteps();return `<div class="workflow-toolbar"><div><div class="eyebrow">WORKFLOW STUDIO</div><h2 style="margin:4px 0">Visual workflow editor</h2><div class="settings-note">Each organization can control its own order, required/optional steps and operating flow.</div></div><select id="workflow-select">${state.workflows.map(x=>`<option value="${esc(x.id)}" ${w?.id===x.id?'selected':''}>${esc(x.name)} · ${esc(x.transaction_type)}</option>`).join('')}</select></div><section class="workflow-canvas"><div class="workflow-canvas-head"><div><div class="eyebrow">${esc(w?.transaction_type||'WORKFLOW')}</div><h2>${esc(w?.name||'No workflow')}</h2><p>Guided mode reads this sequence. Record mode remains the complete transaction record.</p></div><label style="font-size:11px;color:var(--muted)"><input type="checkbox" id="workflow-active" ${w?.is_active?'checked':''}> Active</label></div>${w?`<div class="workflow-chain">${steps.map((s,i)=>`${i?'<span class="workflow-edge"></span>':''}<article class="workflow-node ${s.enabled===false?'disabled':''} ${s.required?'required':''}" data-step="${esc(s.key)}"><div class="workflow-node-top"><div><div class="workflow-node-type">${esc(s.type||'task')}</div><h4>${esc(s.label||s.key)}</h4></div><button data-edit-step="${esc(s.key)}" title="Edit">•••</button></div><p>${esc(s.description||'')}</p><div class="workflow-node-meta"><span class="workflow-pill ${s.required?'req':''}">${s.required?'Required':'Optional'}</span>${s.condition?'<span class="workflow-pill">Conditional</span>':''}${s.enabled===false?'<span class="workflow-pill">Disabled</span>':''}</div><div class="workflow-node-actions"><button data-move="up" data-step="${esc(s.key)}" ${i===0?'disabled':''}>←</button><button data-move="down" data-step="${esc(s.key)}" ${i===steps.length-1?'disabled':''}>→</button><button data-toggle-required="${esc(s.key)}">${s.required?'Optional':'Required'}</button><button data-toggle-enabled="${esc(s.key)}">${s.enabled===false?'Enable':'Disable'}</button></div></article>`).join('')}<button class="workflow-add" id="workflow-add-step">＋ Add step</button></div><div class="workflow-legend"><span>Blue border = required</span><span>Faded = disabled</span><span>Guided mode follows this order</span></div><div id="workflow-inspector"></div>`:'<div class="workflow-empty">No workflow is configured for this organization.</div>'}</section>${w?'<div class="settings-savebar"><button class="primary" id="save-workflow">Save workflow</button></div>':''}`}
function stepInspector(key){const s=normalizedSteps().find(x=>x.key===key);if(!s)return'';return `<section class="workflow-inspector" data-inspector="${esc(key)}"><h3>Edit step</h3><div class="workflow-inspector-grid"><label>Label<input id="wf-label" value="${esc(s.label||'')}"></label><label>Type<select id="wf-type">${['task','capture','verification','event','form','review','notification'].map(x=>`<option ${s.type===x?'selected':''}>${x}</option>`).join('')}</select></label><label style="grid-column:1/-1">Description<textarea id="wf-description">${esc(s.description||'')}</textarea></label><label><input type="checkbox" id="wf-required" ${s.required?'checked':''}> Required</label><label><input type="checkbox" id="wf-enabled" ${s.enabled!==false?'checked':''}> Enabled</label></div><div class="settings-savebar" style="position:static;margin-top:12px;padding:0;border:0;background:transparent"><button id="wf-delete">Delete step</button><button class="primary" id="wf-apply">Apply</button></div></section>`}
function renderWorkflowInspector(key){state.selectedStep=key;const host=document.getElementById('workflow-inspector');if(!host)return;host.innerHTML=stepInspector(key);document.getElementById('wf-apply')?.addEventListener('click',()=>{const steps=[...(state.workflow.steps||[])],i=steps.findIndex(x=>x.key===key);if(i<0)return;steps[i]={...steps[i],label:document.getElementById('wf-label').value.trim()||steps[i].label,type:document.getElementById('wf-type').value,description:document.getElementById('wf-description').value.trim(),required:document.getElementById('wf-required').checked,enabled:document.getElementById('wf-enabled').checked};state.workflow={...state.workflow,steps};renderBody()});document.getElementById('wf-delete')?.addEventListener('click',()=>{state.workflow={...state.workflow,steps:(state.workflow.steps||[]).filter(x=>x.key!==key)};state.selectedStep=null;renderBody()})}
function moveStep(key,dir){const a=normalizedSteps(),i=a.findIndex(x=>x.key===key),j=dir==='up'?i-1:i+1;if(i<0||j<0||j>=a.length)return;[a[i],a[j]]=[a[j],a[i]];a.forEach((x,n)=>x.order=(n+1)*10);state.workflow={...state.workflow,steps:a};renderBody()}
function toggleStep(key,prop){const a=(state.workflow.steps||[]).map(x=>x.key===key?{...x,[prop]:prop==='required'?!x.required:x.enabled===false}:x);state.workflow={...state.workflow,steps:a};renderBody()}
function addStep(){const n=(state.workflow.steps||[]).length+1,key=`custom_${Date.now()}`;state.workflow={...state.workflow,steps:[...(state.workflow.steps||[]),{key,label:`New step ${n}`,description:'Configure this workflow step.',order:n*10,required:false,enabled:true,type:'task',action:key}]};renderBody();setTimeout(()=>renderWorkflowInspector(key),0)}
function bindWorkflows(){document.getElementById('workflow-select')?.addEventListener('change',e=>{state.workflow=state.workflows.find(x=>x.id===e.target.value)||null;state.selectedStep=null;renderBody()});main.querySelectorAll('[data-edit-step]').forEach(b=>b.onclick=()=>renderWorkflowInspector(b.dataset.editStep));main.querySelectorAll('[data-move]').forEach(b=>b.onclick=()=>moveStep(b.dataset.step,b.dataset.move));main.querySelectorAll('[data-toggle-required]').forEach(b=>b.onclick=()=>toggleStep(b.dataset.toggleRequired,'required'));main.querySelectorAll('[data-toggle-enabled]').forEach(b=>b.onclick=()=>toggleStep(b.dataset.toggleEnabled,'enabled'));document.getElementById('workflow-add-step')?.addEventListener('click',addStep);document.getElementById('save-workflow')?.addEventListener('click',saveWorkflow)}
function renderBody(){const body=document.getElementById('settings-body');if(!body)return;if(state.tab==='units')body.innerHTML=units();else if(state.tab==='numbers')body.innerHTML=numbers();else body.innerHTML=workflows();if(state.tab==='units'){document.getElementById('set-system').onchange=e=>{if(e.target.value==='IMPERIAL'){document.getElementById('set-weight').value='LB';document.getElementById('set-dimension').value='IN';document.getElementById('set-temperature').value='F';document.getElementById('set-distance').value='MI'}if(e.target.value==='METRIC'){document.getElementById('set-weight').value='KG';document.getElementById('set-dimension').value='CM';document.getElementById('set-temperature').value='C';document.getElementById('set-distance').value='KM'}};document.getElementById('save-units').onclick=saveUnits}else if(state.tab==='numbers'){main.querySelectorAll('.number-row input,.number-row select').forEach(el=>el.oninput=updatePreview);document.getElementById('save-numbers').onclick=saveNumbers}else bindWorkflows()}
async function saveUnits(){const b=document.getElementById('save-units');b.disabled=true;b.textContent='Saving…';const row={organization_id:state.org,profile_name:state.profile.profile_name||'Default',system_code:document.getElementById('set-system').value,is_default:true,weight_unit:document.getElementById('set-weight').value,dimension_unit:document.getElementById('set-dimension').value,volume_unit:document.getElementById('set-volume').value,temperature_unit:document.getElementById('set-temperature').value,distance_unit:document.getElementById('set-distance').value,default_quantity_uom:document.getElementById('set-qty').value,currency_code:document.getElementById('set-currency').value.trim().toUpperCase()||'USD',rounding:{weight:+document.getElementById('set-rw').value,dimension:+document.getElementById('set-rd').value,volume:+document.getElementById('set-rv').value,currency:+document.getElementById('set-rc').value},updated_at:new Date().toISOString()};const{error}=await supabase.from('organization_unit_profiles').upsert(row,{onConflict:'organization_id,profile_name'});if(error){alert(error.message);b.disabled=false;b.textContent='Save system units';return}state.profile=row;b.textContent='Saved ✓';setTimeout(()=>b.textContent='Save system units',1200);b.disabled=false}
function rowData(row){const id=row.dataset.scheme,s={...state.schemes.find(x=>x.id===id)};row.querySelectorAll('[data-k]').forEach(el=>s[el.dataset.k]=el.type==='checkbox'?el.checked:el.type==='number'?Number(el.value):el.value);return s}
function updatePreview(e){const row=e.target.closest('.number-row'),s=rowData(row);row.querySelector('[data-preview]').textContent='Preview · '+sample(s)}
async function saveNumbers(){const b=document.getElementById('save-numbers');b.disabled=true;b.textContent='Saving…';for(const row of main.querySelectorAll('.number-row')){const s=rowData(row);const{error}=await supabase.from('document_number_schemes').update({prefix:s.prefix,suffix:s.suffix,format_template:s.format_template,date_format:s.date_format,sequence_padding:s.sequence_padding,reset_period:s.reset_period,starting_number:s.starting_number,allow_manual_override:s.allow_manual_override,is_active:s.is_active,updated_at:new Date().toISOString()}).eq('id',s.id);if(error){alert(error.message);b.disabled=false;b.textContent='Save numbering';return}}b.textContent='Saved ✓';setTimeout(()=>b.textContent='Save numbering',1200);b.disabled=false}
async function saveWorkflow(){const b=document.getElementById('save-workflow');if(!b||!state.workflow)return;b.disabled=true;b.textContent='Saving…';const steps=normalizedSteps().map((x,i)=>({...x,order:(i+1)*10}));const {data,error}=await supabase.from('workflow_definitions').update({steps,is_active:document.getElementById('workflow-active')?.checked??true,updated_at:new Date().toISOString()}).eq('id',state.workflow.id).select().single();if(error){alert(error.message);b.disabled=false;b.textContent='Save workflow';return}state.workflow=data;state.workflows=state.workflows.map(x=>x.id===data.id?data:x);b.textContent='Saved ✓';setTimeout(()=>b.textContent='Save workflow',1200);b.disabled=false}
export async function openSystemSettings(tab='units'){state.tab=tab;try{await load();shell()}catch(e){main.innerHTML=`<div class="eyebrow">SETTINGS</div><h1 class="title">System setup</h1><div class="notice warning">${esc(e.message)}</div>`}}
window.nodaraSystemSettings=openSystemSettings;

function injectSettingsLinks(){document.querySelectorAll('.rail-flyout[data-module="settings"] .rail-flyout-list,.mobile-module-sheet .mobile-sheet-list').forEach(list=>{if(list.dataset.systemLinks==='1')return;const looksSettings=[...list.querySelectorAll('button span')].some(x=>x.textContent==='Lists & Options');if(!looksSettings)return;list.dataset.systemLinks='1';const a=document.createElement('button');a.innerHTML='<span>System & Units</span><i>›</i>';a.onclick=()=>{document.querySelector('.rail-flyout,.mobile-module-sheet')?.remove();openSystemSettings('units')};const n=document.createElement('button');n.innerHTML='<span>Document Numbers</span><i>›</i>';n.onclick=()=>{document.querySelector('.rail-flyout,.mobile-module-sheet')?.remove();openSystemSettings('numbers')};const w=document.createElement('button');w.innerHTML='<span>Workflows</span><i>›</i>';w.onclick=()=>{document.querySelector('.rail-flyout,.mobile-module-sheet')?.remove();openSystemSettings('workflows')};list.prepend(w);list.prepend(n);list.prepend(a)})}
new MutationObserver(injectSettingsLinks).observe(document.body,{childList:true,subtree:true});setTimeout(injectSettingsLinks,500);

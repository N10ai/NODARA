import { supabase } from './supabase-client.js';
import { mountCargoWorkspace } from './cargo-workspace.js';
import './transaction-cargo-table-polish.js';
const main=document.getElementById('main');let busy=false,lastKey='';
function hideLegacyTotals(){for(const el of main.querySelectorAll('.detail-grid>div')){const s=el.querySelector('span');if(s?.textContent?.trim()==='Cargo')el.style.display='none'}for(const f of main.querySelectorAll('.field')){const l=f.querySelector('label')?.textContent?.trim();if(['Pieces','Weight','Weight unit','Volume CBM'].includes(l))f.style.display='none'}}
function addCargoNote(kind){if(!main.querySelector('[id$="-save"]')||main.querySelector('.cargo-derived-note'))return;const n=document.createElement('div');n.className='notice compact cargo-derived-note';n.textContent=`Cargo can be created directly on this ${kind} or linked from warehouse cargo. A Warehouse Receipt is only required when cargo enters warehouse custody.`;main.querySelector('.record-section')?.prepend(n)}
async function mountShipment(){const title=main.querySelector('.record-header .title')?.textContent?.trim(),eye=main.querySelector('.record-header .eyebrow')?.textContent?.trim()||'';if(!title||!/^Operations · (AIR|OCEAN|GROUND)$/i.test(eye))return false;const{data:r}=await supabase.from('shipments').select('id,shipment_number').eq('shipment_number',title).maybeSingle();if(!r)return false;const key=`SHIPMENT:${r.id}`;if(lastKey===key&&main.querySelector('[data-canonical-cargo]'))return true;const first=main.querySelector('.record-section');if(!first)return false;const sec=document.createElement('section');sec.className='record-section';sec.dataset.canonicalCargo='';sec.innerHTML='<div data-cargo-mount></div>';first.insertAdjacentElement('afterend',sec);await mountCargoWorkspace(sec.querySelector('[data-cargo-mount]'),{transactionType:'SHIPMENT',transactionId:r.id,title:'Shipment Cargo'});lastKey=key;hideLegacyTotals();addCargoNote('Shipment');return true}
async function mountTransport(){const title=main.querySelector('.record-header .title')?.textContent?.trim(),eye=main.querySelector('.record-header .eyebrow')?.textContent?.trim()||'';if(!title||!/^Operations · (PICKUP|DELIVERY|TRANSFER|DRAYAGE)$/i.test(eye))return false;const{data:r}=await supabase.from('transport_orders').select('id,order_number').eq('order_number',title).maybeSingle();if(!r)return false;const key=`TRANSPORT_ORDER:${r.id}`;if(lastKey===key&&main.querySelector('[data-canonical-cargo]'))return true;const first=main.querySelector('.record-section');if(!first)return false;const sec=document.createElement('section');sec.className='record-section';sec.dataset.canonicalCargo='';sec.innerHTML='<div data-cargo-mount></div>';first.insertAdjacentElement('afterend',sec);await mountCargoWorkspace(sec.querySelector('[data-cargo-mount]'),{transactionType:'TRANSPORT_ORDER',transactionId:r.id,title:'Transport Cargo'});lastKey=key;hideLegacyTotals();addCargoNote('Transport Order');return true}
async function mountCR(){const num=main.querySelector('.record-number')?.textContent?.trim();if(!num)return false;const{data:r}=await supabase.from('cargo_releases').select('id,release_number').eq('release_number',num).maybeSingle();if(!r)return false;const key=`CARGO_RELEASE:${r.id}`;if(lastKey===key&&main.querySelector('[data-canonical-cargo]'))return true;const old=[...main.querySelectorAll('.record-section')].find(s=>/cargo/i.test(s.querySelector('h3')?.textContent||''));if(!old)return false;const sec=document.createElement('section');sec.className='record-section';sec.dataset.canonicalCargo='';sec.innerHTML='<div data-cargo-mount></div>';old.insertAdjacentElement('afterend',sec);await mountCargoWorkspace(sec.querySelector('[data-cargo-mount]'),{transactionType:'CARGO_RELEASE',transactionId:r.id,title:'Cargo / Items',allowAdd:false});lastKey=key;return true}
async function mountWR(){const num=main.querySelector('.wr-modern-title .title')?.textContent?.trim()||main.querySelector('.record-number')?.textContent?.trim();if(!num||!/^WR/i.test(num))return false;const{data:r}=await supabase.from('warehouse_receipts').select('id,receipt_number').eq('receipt_number',num).maybeSingle();if(!r)return false;const key=`WAREHOUSE_RECEIPT:${r.id}`;if(lastKey===key&&main.querySelector('[data-canonical-cargo]'))return true;const sections=[...main.querySelectorAll('.record-section')],anchor=sections.find(s=>/cargo/i.test(s.querySelector('h3')?.textContent||''))||sections[0];if(!anchor)return false;const sec=document.createElement('section');sec.className='record-section canonical-wr-cargo';sec.dataset.canonicalCargo='';sec.innerHTML='<div data-cargo-mount></div>';anchor.insertAdjacentElement('afterend',sec);await mountCargoWorkspace(sec.querySelector('[data-cargo-mount]'),{transactionType:'WAREHOUSE_RECEIPT',transactionId:r.id,title:'Cargo / Items',allowAdd:false});lastKey=key;return true}
async function tick(){if(busy)return;busy=true;try{hideLegacyTotals();if(main.querySelector('[data-canonical-cargo]'))return;await mountShipment()||await mountTransport()||await mountCR()||await mountWR()}catch(e){if(!/does not exist|schema cache/i.test(e?.message||''))console.warn('Transaction cargo workspace',e)}finally{busy=false}}
new MutationObserver(()=>setTimeout(tick,80)).observe(main,{childList:true,subtree:true});setTimeout(tick,800);

// Unified transaction workspace is deliberately lazy/non-critical. A failure here
// must never prevent the core application from booting.
setTimeout(()=>{
  if(!document.querySelector('link[data-txw-css]')){const l=document.createElement('link');l.rel='stylesheet';l.href='./transaction-workspace.css?v=20260907-workspace1';l.dataset.txwCss='1';document.head.appendChild(l)}
  import('./transaction-workspace-shell.js?v=20260907-workspace1').catch(e=>console.warn('[NODARA] optional transaction workspace did not load',e));
},1400);

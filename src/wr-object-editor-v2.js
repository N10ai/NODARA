import { listEntities,listInventoryItems,listEntityItems,findExistingReceiptReference,createWarehouseReceiptObjectV2 } from './wr-data-v2.js?v=20260901-2110';
import { makePackage,makeItem,repeatPackage } from './cargo-tree-builder.js';
const TYPES=['PALLET','CRATE','SKID','BOX','CARTON','BAG','ENVELOPE','DRUM','BUNDLE','TOTE','LOOSE PIECE','CONTAINER'];
const REFS=['PO','BOL','PRO','TRACKING','SO','ASN','CUSTOMER_REF'];
const PARTY_ROLES=[['customer','Customer / Account'],['shipper','Shipper'],['consignee','Consignee'],['carrier','Carrier'],['driver','Driver'],['billing_party','Billing Party']];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function createWRObjectEditorV2({main}){
 let s={description:'',entities:[],catalog:[],parties:{},refs:[{type:'CUSTOMER_REF',value:'',primary:true}],outerType:'PALLET',outerQty:1,innerType:'',innerQty:0,mode:'items',items:[]};
 async function start(){s={description:'',entities:await listEntities(),catalog:await listInventoryItems(),parties:{},refs:[{type:'CUSTOMER_REF',value:'',primary:true}],outerType:'PALLET',outerQty:1,innerType:'',innerQty:0,mode:'items',items:[]};render()}
 const entityOptions=(role)=>`<option value="">Not assigned</option>${s.entities.map(e=>`<option value="${e.id}" ${s.parties[role]?.entity_id===e.id?'selected':''}>${esc(e.name)}${e.code?' · '+esc(e.code):''}</option>`).join('')}`;
 function render(){main.innerHTML=`<div class="wr-object-head"><div><div class="eyebrow">Warehouse Receipt · Draft</div><h1 class="title">New Warehouse Receipt</h1></div><span class="status-pill draft">Draft</span></div><div class="record-tabs"><button data-jump="overview" class="active">Overview</button><button data-jump="references">References <span>${s.refs.filter(r=>r.value).length}</span></button><button data-jump="cargo">Cargo</button><button data-jump="documents">Documents</button><button data-jump="charges">Charges</button></div><div class="wr-object-layout"><div class="wr-object-main">${overview()}${references()}${cargo()}${documents()}${charges()}</div><aside class="wr-draft-summary">${summary()}</aside></div><div class="wr-savebar"><button class="secondary" id="wr-cancel">Cancel</button><span id="wr-save-status"></span><button class="primary" id="wr-save">Create Warehouse Receipt</button></div>`;bind()}
 function overview(){return `<section class="record-section wr-object-section" id="overview"><div class="section-heading"><div><span class="section-kicker">01</span><h3>Parties & Overview</h3></div><span class="section-state">Saved entities first</span></div><div class="party-grid">${PARTY_ROLES.map(([role,label])=>`<div class="field object-field"><label>${label}</label><select data-party="${role}">${entityOptions(role)}</select><small>${role==='driver'?'Saved driver/contact or leave blank':'Choose a saved entity'}</small></div>`).join('')}</div><div class="field object-field"><label>Description / Commodity</label><input id="wr-description" value="${esc(s.description)}" placeholder="Overall receipt description"></div></section>`}
 function references(){return `<section class="record-section wr-object-section" id="references"><div class="section-heading"><div><span class="section-kicker">02</span><h3>References</h3></div><button class="secondary compact-btn" id="add-ref">＋ Add</button></div><div class="object-list">${s.refs.map((r,i)=>`<div class="object-line"><select data-ref-type="${i}">${REFS.map(t=>`<option ${t===r.type?'selected':''}>${t}</option>`).join('')}</select><input data-ref-value="${i}" value="${esc(r.value)}" placeholder="Scan or type"><label class="primary-ref"><input type="radio" name="primary" data-ref-primary="${i}" ${r.primary?'checked':''}> Primary</label><button class="subtle" data-ref-remove="${i}">×</button></div>`).join('')}</div></section>`}
 function cargo(){return `<section class="record-section wr-object-section" id="cargo"><div class="section-heading"><div><span class="section-kicker">03</span><h3>Cargo</h3></div><span class="section-state">${s.outerQty} ${esc(s.outerType)}</span></div><div class="cargo-builder-row"><div><label>Outer package</label><select id="outer-type">${TYPES.map(t=>`<option ${t===s.outerType?'selected':''}>${t}</option>`).join('')}</select></div><div><label>Qty</label><input id="outer-qty" type="number" min="1" value="${s.outerQty}"></div><div><label>Inside each</label><select id="inner-type"><option value="">No inner package</option>${TYPES.map(t=>`<option ${t===s.innerType?'selected':''}>${t}</option>`).join('')}</select></div><div><label>Qty inside</label><input id="inner-qty" type="number" min="0" value="${s.innerQty||''}" ${s.innerType?'':'disabled'}></div></div><div class="mode-switch"><button data-mode="items" class="${s.mode==='items'?'selected':''}">Track item contents</button><button data-mode="package" class="${s.mode==='package'?'selected':''}">Package-level only</button></div>${s.mode==='items'?itemEditor():'<div class="notice">Only the physical packages will be tracked. SKU/unit inventory can be added later.</div>'}</section>`}
 function itemEditor(){return `<div class="item-builder"><div class="item-picker"><div class="field"><label>Add saved item</label><select id="item-select"><option value="">Choose part / SKU</option>${s.catalog.map(x=>`<option value="${x.id}">${esc(x.part_number||x.sku||'Item')} · ${esc(x.description||'')}</option>`).join('')}</select></div><button class="secondary compact-btn" id="add-item">＋ Add item</button></div><div class="object-list">${s.items.length?s.items.map((x,i)=>`<div class="item-object"><div><b>${esc(x.display_part||x.part_number||x.sku||'Item')}</b><small>${esc(x.description||'')}</small></div><div class="field"><label>Qty per ${esc((s.innerType||s.outerType).toLowerCase())}</label><input data-item-qty="${i}" type="number" min="0.0001" step="any" value="${x.qty}"></div>${x.lot_tracking?`<div class="field"><label>Lot</label><input data-item-lot="${i}" value="${esc(x.lot||'')}"></div>`:''}${x.serial_tracking?`<div class="field"><label>Serial</label><input data-item-serial="${i}" value="${esc(x.serial||'')}"></div>`:''}<button class="subtle" data-item-remove="${i}">×</button></div>`).join(''):'<div class="empty compact">No item contents added yet.</div>'}</div></div>`}
 function documents(){return `<section class="record-section wr-object-section" id="documents"><div class="section-heading"><div><span class="section-kicker">04</span><h3>Documents</h3></div><span class="section-state">Optional</span></div><div class="module-inline"><button class="secondary" id="doc-placeholder">＋ Attach document</button><span class="muted">BOL, packing list, POD, photos, TSA/bond/compliance documents.</span></div></section>`}
 function charges(){return `<section class="record-section wr-object-section" id="charges"><div class="section-heading"><div><span class="section-kicker">05</span><h3>Charges</h3></div><span class="section-state">Optional</span></div><div class="module-inline"><button class="secondary" id="charge-placeholder">＋ Add charge</button><span class="muted">Receiving, handling, storage and accessorials stay on this WR.</span></div></section>`}
 function summary(){const party=(r)=>s.entities.find(e=>e.id===s.parties[r]?.entity_id)?.name||'—';return `<div class="eyebrow">Draft summary</div><h3>${esc(party('customer'))}</h3><div class="summary-line"><span>Shipper</span><b>${esc(party('shipper'))}</b></div><div class="summary-line"><span>Consignee</span><b>${esc(party('consignee'))}</b></div><div class="summary-line"><span>Carrier</span><b>${esc(party('carrier'))}</b></div><div class="summary-line"><span>References</span><b>${s.refs.filter(r=>r.value).length}</b></div><div class="summary-line"><span>Cargo</span><b>${s.outerQty} ${esc(s.outerType)}</b></div><div class="summary-line"><span>Items</span><b>${s.mode==='package'?'Package only':s.items.length}</b></div>`}
 function bind(){document.querySelectorAll('[data-jump]').forEach(b=>b.onclick=()=>document.getElementById(b.dataset.jump)?.scrollIntoView({behavior:'smooth'}));document.querySelectorAll('[data-party]').forEach(el=>el.onchange=async()=>{sync();const role=el.dataset.party;s.parties[role]=el.value?{entity_id:el.value}:null;if(role==='customer'&&el.value){try{const p=await listEntityItems(el.value);if(p.length)s.catalog=p.map(x=>({...x.inventory_items,id:x.inventory_item_id,display_part:x.customer_part_number||x.inventory_items?.part_number,display_sku:x.customer_sku||x.inventory_items?.sku,description:x.description_override||x.inventory_items?.description,lot_tracking:x.lot_tracking_override??x.inventory_items?.lot_tracking,serial_tracking:x.serial_tracking_override??x.inventory_items?.serial_tracking}))}catch{}}render()});document.getElementById('add-ref').onclick=()=>{sync();s.refs.push({type:'PO',value:'',primary:false});render()};document.querySelectorAll('[data-ref-remove]').forEach(b=>b.onclick=()=>{sync();s.refs.splice(+b.dataset.refRemove,1);if(!s.refs.length)s.refs=[{type:'CUSTOMER_REF',value:'',primary:true}];if(!s.refs.some(r=>r.primary))s.refs[0].primary=true;render()});document.getElementById('inner-type').onchange=()=>{sync();render()};document.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{sync();s.mode=b.dataset.mode;render()});document.getElementById('add-item')?.addEventListener('click',()=>{sync();const id=document.getElementById('item-select').value,x=s.catalog.find(i=>String(i.id)===id);if(x&&!s.items.some(i=>i.id===x.id))s.items.push({...x,qty:1,lot:'',serial:''});render()});document.querySelectorAll('[data-item-remove]').forEach(b=>b.onclick=()=>{sync();s.items.splice(+b.dataset.itemRemove,1);render()});document.getElementById('wr-save').onclick=save;document.getElementById('wr-cancel').onclick=()=>window.nodaraWRList?.();document.getElementById('doc-placeholder').onclick=()=>alert('Document upload is the next functional module.');document.getElementById('charge-placeholder').onclick=()=>alert('Charge entry is the next functional module.')}
 function sync(){s.description=document.getElementById('wr-description')?.value??s.description;s.outerType=document.getElementById('outer-type')?.value??s.outerType;s.outerQty=Number(document.getElementById('outer-qty')?.value||s.outerQty||0);s.innerType=document.getElementById('inner-type')?.value??s.innerType;s.innerQty=s.innerType?Number(document.getElementById('inner-qty')?.value||s.innerQty||0):0;document.querySelectorAll('[data-party]').forEach(el=>s.parties[el.dataset.party]=el.value?{entity_id:el.value}:null);s.refs=s.refs.map((r,i)=>({...r,type:document.querySelector(`[data-ref-type="${i}"]`)?.value||r.type,value:document.querySelector(`[data-ref-value="${i}"]`)?.value?.trim?.()||'',primary:!!document.querySelector(`[data-ref-primary="${i}"]`)?.checked}));s.items=s.items.map((x,i)=>({...x,qty:Number(document.querySelector(`[data-item-qty="${i}"]`)?.value||x.qty||0),lot:document.querySelector(`[data-item-lot="${i}"]`)?.value?.trim?.()||'',serial:document.querySelector(`[data-item-serial="${i}"]`)?.value?.trim?.()||''}))}
 function validate(){if(!Number.isInteger(s.outerQty)||s.outerQty<1)return'Outer package quantity must be at least 1.';if(s.innerType&&(!Number.isInteger(s.innerQty)||s.innerQty<1))return'Inner package quantity must be at least 1.';if(s.mode==='items'&&!s.items.length)return'Add at least one saved item, or choose Package-level only.';for(const x of s.items){if(!(x.qty>0))return`${x.part_number||x.sku||'Item'} needs a quantity greater than zero.`;if(x.serial_tracking&&(x.qty!==1||!x.serial))return`${x.part_number||x.sku||'Serialized item'} must be quantity 1 with a serial number.`;if(x.lot_tracking&&!x.lot)return`${x.part_number||x.sku||'Lot item'} needs a lot number.`}return''}
 function tree(){const itemNodes=s.mode==='items'?s.items.map(x=>makeItem({sku:x.sku||null,part_number:x.part_number||null,description:x.description||null,quantity:x.qty,uom:x.base_uom||'EA',lot_number:x.lot||null,serial_number:x.serial||null})):[];if(s.innerType){const inner=()=>makePackage(s.innerType,1,itemNodes.map(x=>structuredClone(x)));return repeatPackage(s.outerType,s.outerQty,Array.from({length:s.innerQty},inner))}return repeatPackage(s.outerType,s.outerQty,itemNodes)}
 async function save(){sync();const out=document.getElementById('wr-save-status'),err=validate();if(err){out.innerHTML=`<span class="warning">${esc(err)}</span>`;return}for(const r of s.refs.filter(r=>r.value)){try{const d=await findExistingReceiptReference(r.value);if(d.length&&!confirm(`${r.type} ${r.value} already exists on another WR. Create anyway?`))return}catch{}}const btn=document.getElementById('wr-save');btn.disabled=true;btn.textContent='Creating…';try{const customer=s.parties.customer?.entity_id||null;const result=await createWarehouseReceiptObjectV2({customer_id:customer,description:s.description,references:s.refs.filter(r=>r.value),parties:s.parties,tree:tree()});window.nodaraWROpen?.(result.warehouse_receipt_id)}catch(e){btn.disabled=false;btn.textContent='Create Warehouse Receipt';out.innerHTML=`<span class="warning">${esc(e.message)}</span>`}}
 return{start}
}
